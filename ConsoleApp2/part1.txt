#region Part1Q1
/*
 * abstraction is the process of exposing the what the user needs and hiding how it is done
 * abstraction is about hiding how the process is done, encapsulation is about controlling the access to the data
 * abstraction: the atm gives you different option and operation but u dnt know how those operations work inside of the machine
 * encapsulation: u can not access the data inside the atm 
 */
#endregion

#region Part1Q2
/*
 * abstract class in used when there is a "is-a" relation while the interface is used to add a apability
 * the abstract class can has constructor and fields
 * interface represent a full abstract while the abstract class represent a partial abstraction
 * we can use multiple inhertance whith the interface but not the abstract class
 * i will choose interface if there is a capability that needs to be added to some classes
 * but use the abstract class if there is a common behavior between all classes which fall under the same category "is-a" 
 */
#endregion

#region Part1Q3
/*
 * A) because appliance is an abstract class and we can not create an object off it
 * B) powerconsumption is an abstract method: it has to be overridden and implemented in the child class, status is a virtual method with implementation for it so it can be overridden but still it already has its own implementation
 *    ,label is a normal method which gonna be inherited from the abstract class to the child class
 * powerconsumption is abstracted because it will has deverent implementation accross differenct class, status is virtual because it can be changed but still it has a default, label will has a fixed logic that will not change
 * C) "standby" because it was not overrideen by the toaster class
 */
#endregion

#region Part1Q4
/*
 * A) a partial class allows a class to be split into multiple files, but at compile time the compiler combines them into one single class
 * Improve organization, Allow multiple developers to work on the same class
 * B) A partial method is a method that is declared in one partial class, optionally implemented in another partial class
 * The code will still compile because it does not has to be implemented
 * C) An extension method allows you to add methods to an existing class without modifying it.
 * D) $20.00
 */
#endregion